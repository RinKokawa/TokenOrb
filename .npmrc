fund=false
audit=false
loglevel=warn

# Allow native build scripts for electron / esbuild / installer
# (npm 11+ blocks postinstall by default; these packages need it)
allowed-build-scripts[]=electron
allowed-build-scripts[]=electron-winstaller
allowed-build-scripts[]=esbuild